import { categories, careerGoals } from '../data/options';
const FilterBar = ({ filters, setFilters }) => <div className="glass mb-8 grid gap-3 rounded-3xl p-4 md:grid-cols-5"><input className="input md:col-span-2" placeholder="Search courses, skills, technologies..." value={filters.search || ''} onChange={e => setFilters({ ...filters, search: e.target.value })}/><select className="input" value={filters.category || ''} onChange={e => setFilters({ ...filters, category: e.target.value })}><option value="">All categories</option>{categories.map(item => <option className="bg-slate-900" key={item}>{item}</option>)}</select><select className="input" value={filters.difficulty || ''} onChange={e => setFilters({ ...filters, difficulty: e.target.value })}><option value="">All levels</option>{['Beginner','Intermediate','Advanced'].map(item => <option className="bg-slate-900" key={item}>{item}</option>)}</select><select className="input" value={filters.careerPath || ''} onChange={e => setFilters({ ...filters, careerPath: e.target.value })}><option value="">Career path</option>{careerGoals.map(item => <option className="bg-slate-900" key={item}>{item}</option>)}</select></div>;
export default FilterBar;
