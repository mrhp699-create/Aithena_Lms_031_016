import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { BrainCircuit } from 'lucide-react';
import GlassCard from '../components/GlassCard';
import { useAuth } from '../context/AuthContext';
const LoginPage = () => { const [form,setForm]=useState({email:'student@edupath.ai',password:'password123'}); const [error,setError]=useState(''); const { login }=useAuth(); const navigate=useNavigate(); const submit=async e=>{e.preventDefault(); setError(''); try{const user=await login(form); navigate(user.role==='admin'?'/admin':'/dashboard');}catch(err){setError(err.response?.data?.message||'Login failed');}}; return <div className="page grid place-items-center"><GlassCard className="w-full max-w-md p-8"><BrainCircuit className="mx-auto text-cyan-300" size={42}/><h1 className="mt-4 text-center text-3xl font-black">Login to Aithena AI</h1><p className="mt-2 text-center text-slate-400">Student: student@edupath.ai / password123<br/>Admin: admin@edupath.ai / admin123</p><form onSubmit={submit} className="mt-6 space-y-4"><input className="input" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" placeholder="Password" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/>{error&&<p className="text-sm text-rose-300">{error}</p>}<button className="btn btn-primary w-full">Login</button></form><p className="mt-5 text-center text-sm text-slate-400">No account? <Link to="/register" className="text-cyan-300">Register</Link></p></GlassCard></div> };
export default LoginPage;
